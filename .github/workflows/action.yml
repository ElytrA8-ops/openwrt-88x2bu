name: Make OpenWRT 88x2bu image
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Install Requirements
        run: |
          sudo apt install neofetch
      - name: Run a multi-line script
        run: |
          neofetch
